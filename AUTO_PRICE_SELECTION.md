# Автоматический подбор цены запчасти

## Описание

При создании заказа система автоматически подставляет цену запчасти в зависимости от типа цены, назначенного клиенту.

## Как это работает

### 1. Структура данных

#### Clients (Клиенты)
```prisma
model Clients {
  id            String       @id @default(cuid())
  name          String
  fullName      String
  address       String?
  priceAccessId Int?         // ← Тип цены клиента
  priceAccess   PriceTypes?  @relation("ClientPriceAccess", fields: [priceAccessId], references: [id])
}
```

#### Autoparts (Запчасти)
```prisma
model Autoparts {
  id          String              @id @default(cuid())
  article     String
  description String
  prices      AutopartPrices[]    // ← Цены запчасти
}

model AutopartPrices {
  autopart_id   String
  pricesType_id Int               // ← Связь с типом цены
  price         Float             // ← Значение цены
  priceType     PriceTypes        @relation(fields: [pricesType_id], references: [id])
}
```

#### PriceTypes (Типы цен)
```prisma
model PriceTypes {
  id      Int              @id @default(autoincrement())
  name    String           // Например: "Розница", "Опт", "VIP"
  prices  AutopartPrices[]
  clients Clients[]        @relation("ClientPriceAccess")
}
```

### 2. Логика работы

#### При выборе запчасти:

1. Пользователь выбирает клиента
2. Пользователь выбирает запчасть
3. **Система автоматически:**
   - Определяет `priceAccessId` выбранного клиента
   - Ищет в `prices` запчасти цену с соответствующим `pricesType_id`
   - Подставляет найденную цену в поле "Цена"

#### При изменении клиента:

1. Если запчасть уже выбрана
2. **Система автоматически:**
   - Определяет `priceAccessId` нового клиента
   - Обновляет цену запчасти согласно новому типу цены

## Изменения в коде

### 1. Схема базы данных

**Файл:** `lib/db/schema.prisma`

```prisma
// Добавлено поле priceAccessId
model Clients {
  priceAccessId   Int?
  priceAccess     PriceTypes? @relation("ClientPriceAccess", fields: [priceAccessId], references: [id])
}

// Добавлена связь с клиентами
model PriceTypes {
  clients Clients[] @relation("ClientPriceAccess")
}
```

**Миграция:** Выполнено `npx prisma db push`

### 2. TypeScript типы

**Файл:** `app/types/orders.ts`

```typescript
export interface Client {
  priceAccessId: number | null;  // ← Добавлено
  priceAccess?: PriceType;       // ← Добавлено
}

export interface PriceType {     // ← Новый интерфейс
  id: number;
  name: string;
}
```

### 3. API endpoints

#### `/api/clients` - Обновлен GET запрос

**Файл:** `app/api/clients/route.ts`

```typescript
const clients = await prisma.clients.findMany({
  include: {
    priceAccess: true,  // ← Добавлено
    // ...
  },
});
```

#### `/api/autoparts` - Обновлен GET запрос

**Файл:** `app/api/autoparts/route.ts`

```typescript
const autoparts = await db.autoparts.findMany({
  include: {
    prices: {         // ← Добавлено
      include: {
        priceType: true,
      },
    },
    // ...
  },
});
```

### 4. OrderModal - Автоматический подбор цены

**Файл:** `components/orders/OrderModal.tsx`

#### При выборе запчасти:

```typescript
<Select 
  value={selectedAutopartId} 
  onValueChange={(value) => {
    setSelectedAutopartId(value);
    
    // Автоматический подбор цены
    const selectedAutopart = autoparts.find(a => a.id === value);
    const selectedClient = clients.find(c => c.id === clientId);
    
    if (selectedAutopart && selectedClient && selectedClient.priceAccessId && selectedAutopart.prices) {
      const matchingPrice = selectedAutopart.prices.find(
        p => p.pricesType_id === selectedClient.priceAccessId
      );
      
      if (matchingPrice) {
        setPrice(matchingPrice.price);
        console.log(`Автоматически установлена цена: ${matchingPrice.price}`);
      }
    }
  }}
>
```

#### При изменении клиента:

```typescript
<Select 
  value={clientId} 
  onValueChange={(value) => {
    setClientId(value);
    
    // Обновляем цену при изменении клиента (если запчасть уже выбрана)
    if (selectedAutopartId) {
      const selectedAutopart = autoparts.find(a => a.id === selectedAutopartId);
      const selectedClient = clients.find(c => c.id === value);
      
      if (selectedAutopart && selectedClient && selectedClient.priceAccessId && selectedAutopart.prices) {
        const matchingPrice = selectedAutopart.prices.find(
          p => p.pricesType_id === selectedClient.priceAccessId
        );
        
        if (matchingPrice) {
          setPrice(matchingPrice.price);
        }
      }
    }
  }}
>
```

## Пользовательский интерфейс

### В селекте клиента:

```
┌─────────────────────────────────────┐
│ ООО "Рога и копыта"                 │
│ Тип цены: Розница                   │ ← Показывается тип цены
├─────────────────────────────────────┤
│ ИП Иванов                           │
│ Тип цены: Опт                       │
└─────────────────────────────────────┘
```

### В поле цены:

- При выборе запчасти цена **автоматически** подставляется
- Цена соответствует типу цены клиента
- Пользователь может вручную изменить цену при необходимости

## Примеры использования

### Сценарий 1: Создание заказа

1. **Выбираем клиента:** "ООО Рога и копыта" (Тип цены: Розница)
2. **Выбираем запчасть:** "ABC123 - Фильтр масляный"
3. **Цена автоматически:** 500 руб. (розничная цена)

### Сценарий 2: Смена клиента

1. **Выбран клиент:** "ООО Рога и копыта" (Тип: Розница)
2. **Выбрана запчасть:** "ABC123" (Цена: 500 руб.)
3. **Меняем клиента на:** "ИП Иванов" (Тип: Опт)
4. **Цена обновляется на:** 400 руб. (оптовая цена)

### Сценарий 3: У клиента нет типа цены

1. **Выбираем клиента:** "Физ. лицо" (priceAccessId = null)
2. **Выбираем запчасть:** "ABC123"
3. **Цена:** 0 (нужно ввести вручную)

### Сценарий 4: У запчасти нет цены для типа

1. **Выбран клиент:** "ООО Рога и копыта" (Тип: VIP)
2. **Выбрана запчасть:** "DEF456" (нет цены для типа VIP)
3. **Цена:** 0 (нужно ввести вручную)
4. **Консоль:** "Цена для данного типа не найдена"

## Отладка

В консоли браузера выводятся логи:

```javascript
// При успешном подборе цены:
"Автоматически установлена цена: 500 (тип: Розница)"

// При обновлении цены для нового клиента:
"Цена обновлена для нового клиента: 400 (тип: Опт)"

// Если цена не найдена:
"Цена для данного типа не найдена"
```

## Миграция данных

### Для существующих клиентов:

Нужно назначить тип цены через UI управления клиентами:

1. Перейти в раздел "Клиенты"
2. Редактировать клиента
3. Выбрать "Тип цены"
4. Сохранить

### Для существующих запчастей:

Нужно установить цены через UI управления запчастями:

1. Перейти в раздел "Запчасти"
2. Редактировать запчасть
3. Установить цены для разных типов
4. Сохранить

## Преимущества

✅ **Автоматизация** - не нужно вручную искать цену  
✅ **Точность** - исключены ошибки при вводе цены  
✅ **Гибкость** - можно переопределить цену вручную  
✅ **Прозрачность** - тип цены виден при выборе клиента  
✅ **Масштабируемость** - легко добавлять новые типы цен  

## Ограничения

⚠️ Если у клиента не указан `priceAccessId` - цена не подставится  
⚠️ Если у запчасти нет цены для данного типа - цена будет 0  
⚠️ Пользователь может вручную изменить подставленную цену  

## Дальнейшие улучшения

💡 Добавить валидацию: предупреждать, если цена = 0  
💡 Показывать доступные цены для запчасти в селекте  
💡 Добавить историю изменения цен  
💡 Автоматическое обновление цен при изменении прайс-листов  

