# Исправление: Отображение пользователей на странице Клиентов

## Проблема

На странице **Клиенты** отображались только записи из таблицы `Clients`, но не показывались авторизованные пользователи (`User`), которые еще не были связаны с клиентами.

## Решение

Страница теперь показывает **объединенный список**:
1. **Все Clients** (с userId и без)
2. **Все Users**, у которых нет связанного Client

## Что изменилось

### 1. ClientsTable.tsx

#### Добавлена загрузка пользователей:
```typescript
const fetchData = async () => {
  // Загружаем клиентов
  const clientsResponse = await fetch('/api/clients');
  const clientsData = await clientsResponse.json();
  setClients(clientsData);

  // Загружаем пользователей
  const usersResponse = await fetch('/api/users');
  const usersData = await usersResponse.json();
  setUsers(usersData);
};
```

#### Объединение данных:
```typescript
const allItems = React.useMemo(() => {
  // Получаем userId всех клиентов
  const clientUserIds = new Set(clients.filter(c => c.userId).map(c => c.userId));
  
  // Находим пользователей без клиента
  const usersWithoutClient = users.filter(user => !clientUserIds.has(user.id));
  
  // Преобразуем пользователей в формат клиентов
  const userAsClients: Client[] = usersWithoutClient.map(user => ({
    id: `user_${user.id}`,
    name: user.name || user.email,
    fullName: user.name || user.email,
    email: user.email,
    phone: user.phone || null,
    userId: user.id,
    user: { id: user.id, name: user.name, email: user.email, phone: user.phone },
    // ... другие поля
  }));
  
  return [...clients, ...userAsClients];
}, [clients, users, priceTypes]);
```

### 2. API /api/users

Добавлены поля `phone` и `role` в выборку:
```typescript
export async function GET() {
  const users = await db.user.findMany({
    select: {
      id: true,
      name: true,
      email: true,
      phone: true,        // ← Добавлено
      role: true,         // ← Добавлено
      priceAccessId: true,
      warehouseAccessId: true,
      isConfirmed: true,  // ← Добавлено
    },
    orderBy: {
      createdAt: 'desc',
    },
  });
  return NextResponse.json(users);
}
```

### 3. Статистика

Обновлена для отображения правильных данных:
```typescript
Всего записей:           allItems.length
Пользователи (авторизованы): allItems.filter(c => c.userId).length
Только клиенты:          allItems.filter(c => !c.userId).length
С email:                 allItems.filter(c => c.email).length
```

## Как это работает сейчас

### Пример 1: Пользователь без Client

```
User:
- id: "user_123"
- name: "Иван Петров"
- email: "ivan@example.com"
- phone: "+380501234567"

На странице показывается как:
┌─────────────────────────────────┐
│ Иван Петров                     │
│ 📧 ivan@example.com             │
│ 📱 +380501234567                │
│ ✅ Авторизован                  │
└─────────────────────────────────┘
```

### Пример 2: Client с User

```
Client:
- id: "client_456"
- name: "ООО Автосервис"
- userId: "user_789"

User:
- id: "user_789"
- email: "info@autoservice.com"

На странице показывается как:
┌─────────────────────────────────┐
│ ООО Автосервис                  │
│ 📧 info@autoservice.com         │
│ ✅ Авторизован                  │
│    info@autoservice.com         │
└─────────────────────────────────┘
```

### Пример 3: Client без User

```
Client:
- id: "client_999"
- name: "ИП Иванов"
- userId: null

На странице показывается как:
┌─────────────────────────────────┐
│ ИП Иванов                       │
│ —                               │
│ ❌ Не авторизован               │
└─────────────────────────────────┘
```

## Логика отображения

```
                      Страница Клиентов
                             │
           ┌─────────────────┴──────────────────┐
           │                                    │
       Clients                              Users
    ┌──────────┐                       ┌──────────┐
    │ Client 1 │                       │ User 1   │
    │ userId=1 │───связан───┐          │ id=1     │
    └──────────┘            │          └──────────┘
                            │
    ┌──────────┐            │          ┌──────────┐
    │ Client 2 │            │          │ User 2   │
    │ userId=  │            └─────X    │ id=2     │ ← Показать!
    │   null   │                       └──────────┘
    └──────────┘                       
                                       ┌──────────┐
    ┌──────────┐                       │ User 3   │
    │ Client 3 │                       │ id=3     │ ← Показать!
    │ userId=  │                       └──────────┘
    │   null   │                       
    └──────────┘                       

Отображается:
• Client 1 (с User 1)
• Client 2 (без User)
• Client 3 (без User)
• User 2 (без Client) ← НОВОЕ!
• User 3 (без Client) ← НОВОЕ!
```

## Преимущества

✅ **Полная картина** - видны все авторизованные пользователи  
✅ **Удобство** - не нужно переключаться между разделами  
✅ **Связывание** - легко найти пользователей для связывания с клиентами  
✅ **Статистика** - правильный подсчет всех записей  

## Use Cases

### Use Case 1: Пользователь зарегистрировался, но не создан Client

```
Шаг 1: Пользователь регистрируется
       email: "newuser@example.com"
       → User создан

Шаг 2: Администратор открывает страницу Клиентов
       → Видит "newuser@example.com" со статусом "Авторизован"

Шаг 3: Администратор создает для него заказ
       → Выбирает пользователя из списка
       → Или создает Client и связывает с User
```

### Use Case 2: Массовое связывание

```
Администратор видит:
- 5 Clients без авторизации
- 3 Users без Client

Задача: Связать их
1. Найти соответствие по email/phone
2. Использовать API /api/clients/{id}/link-user
3. После связывания User исчезает из списка (уже в Client)
```

## Технические детали

### Идентификация записей:

**Client:**
```typescript
id: "client_abc123"  // Реальный ID из БД
```

**User (без Client):**
```typescript
id: "user_xyz789"    // Префикс "user_" + реальный ID
```

### Фильтрация дубликатов:

```typescript
const clientUserIds = new Set(clients.filter(c => c.userId).map(c => c.userId));
const usersWithoutClient = users.filter(user => !clientUserIds.has(user.id));
```

Это гарантирует, что один User не появится дважды.

## Обновленная статистика

**Было:**
```
Всего клиентов:    12
С авторизацией:     8
Без авторизации:    4
```

**Стало:**
```
Всего записей:          15  (12 clients + 3 users без client)
Пользователи (авториз):  11  (8 clients с userId + 3 users)
Только клиенты:          4   (clients без userId)
С email:                14
```

## Файлы

**Измененные:**
- `components/clients/ClientsTable.tsx` - объединение данных
- `app/api/users/route.ts` - добавлены поля

**Без изменений:**
- `components/clients/ClientModal.tsx` - работает как прежде
- `app/dashboard/clients/page.tsx` - без изменений
- `app/api/clients/route.ts` - без изменений

## Тестирование

### Проверьте:

1. ✅ Создайте пользователя (User) через регистрацию
2. ✅ Откройте `/dashboard/clients`
3. ✅ Убедитесь, что пользователь отображается
4. ✅ Создайте Client с таким же email
5. ✅ Свяжите их через API
6. ✅ Убедитесь, что теперь отображается один раз (как Client с User)

## Следующие шаги

⏳ Добавить кнопку "Создать Client для User" прямо из таблицы  
⏳ Добавить фильтр "Только пользователи без Client"  
⏳ Реализовать UI для связывания Client ↔ User  

---

**Готово! Теперь все пользователи видны на странице Клиентов! ✅**

