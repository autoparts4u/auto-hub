# Быстрый старт: Клиенты и Пользователи

## Что изменилось

Теперь **Clients** (клиенты) и **User** (пользователи) - это разные сущности, которые можно связать.

**До:**
- User = Client (одно и то же)

**Сейчас:**
- **Clients** - клиенты для заказов (могут быть без авторизации)
- **User** - авторизованные пользователи системы
- **Связь** - опциональная, устанавливается администратором

## Основная идея

```
1. Администратор создает клиента вручную
   ↓
2. Клиент работает без авторизации (звонит, администратор создает заказы)
   ↓
3. Клиент регистрируется в системе (когда захочет)
   ↓
4. Администратор связывает Client ↔ User
   ↓
5. Клиент может войти и видеть свои заказы
```

## Изменения в базе данных

### Новые поля в Clients:
```typescript
{
  email: string | null     // Для связи с User
  phone: string | null     // Для связи с User
  userId: string | null    // ID связанного пользователя
  createdAt: DateTime
  updatedAt: DateTime
}
```

### Связь User ↔ Client:
- **Один к одному** (1:1)
- User может быть связан только с одним Client
- Client может быть связан только с одним User

## Как использовать

### 1. Создание клиента (admin)

```typescript
POST /api/clients
{
  "name": "ООО Рога и копыта",
  "fullName": "Общество с ограниченной ответственностью Рога и копыта",
  "email": "info@rk.com",       // ← Важно для связи!
  "phone": "+380501234567",     // ← Или телефон
  "address": "ул. Главная, 123",
  "priceAccessId": 1
}
```

### 2. Клиент регистрируется

Клиент самостоятельно регистрируется через форму регистрации:
- Использует тот же email: `info@rk.com`
- Придумывает пароль

### 3. Связывание (admin)

#### Автоматическое (по email/phone):
```typescript
POST /api/clients/{clientId}/link-user
{
  "autoLink": true
}
```

Система сама найдет User с таким email или phone.

#### Вручную (выбор пользователя):
```typescript
POST /api/clients/{clientId}/link-user
{
  "autoLink": false,
  "userId": "user_id_here"
}
```

### 4. Отвязать пользователя

```typescript
DELETE /api/clients/{clientId}/link-user
```

## API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/clients` | Список клиентов (с user если связан) |
| POST | `/api/clients` | Создать клиента |
| GET | `/api/clients/{id}` | Получить клиента |
| PUT | `/api/clients/{id}` | Обновить клиента |
| DELETE | `/api/clients/{id}` | Удалить клиента (если нет заказов) |
| POST | `/api/clients/{id}/link-user` | Связать с пользователем |
| DELETE | `/api/clients/{id}/link-user` | Отвязать пользователя |

## Примеры

### Пример 1: Типичный workflow

```
📝 Шаг 1: Админ создает клиента
POST /api/clients
{
  "name": "Автомагазин",
  "email": "shop@auto.com"
}
→ Client создан, userId = null

📞 Шаг 2: Клиент звонит, админ создает заказы
(Клиент работает без авторизации)

🔐 Шаг 3: Клиент регистрируется
Заходит на сайт, регистрируется с email shop@auto.com
→ User создан

🔗 Шаг 4: Админ связывает
POST /api/clients/{id}/link-user
{ "autoLink": true }
→ Client.userId = User.id

✅ Результат: Клиент заходит и видит все свои заказы!
```

### Пример 2: Массовый импорт

```
1. Админ импортирует 100 клиентов из Excel:
   - Все с email
   - Все userId = null

2. Клиенты постепенно регистрируются (10 человек)

3. Админ запускает автосвязывание для всех:
   
for each client with email and !userId {
  POST /api/clients/{id}/link-user { "autoLink": true }
}

4. 10 клиентов получают доступ к своим заказам
```

## Проверка

### Клиент связан с пользователем?
```typescript
GET /api/clients/{id}

Response:
{
  "id": "...",
  "name": "ООО Рога и копыта",
  "userId": "user_123",         // ✅ Есть связь
  "user": {                     // ✅ Данные пользователя
    "id": "user_123",
    "email": "info@rk.com",
    "name": "ООО Рога и копыта",
    "role": "user",
    "isConfirmed": true
  }
}
```

### Клиент НЕ связан:
```typescript
{
  "id": "...",
  "name": "ООО Рога и копыта",
  "userId": null,               // ❌ Нет связи
  "user": null                  // ❌ Нет данных
}
```

## Миграция

После обновления схемы:

1. ✅ Все существующие клиенты получат `userId = null`
2. ✅ База данных синхронизирована: `npx prisma db push`
3. ✅ Prisma Client регенерирован

Ничего не сломается! Просто теперь можно связывать клиентов с пользователями.

## Статусы клиентов

| Статус | userId | user | email | Действия |
|--------|--------|------|-------|----------|
| 🆕 Новый | null | null | ✅ | Создан, ждет регистрации |
| 🔗 Связан | ✅ | ✅ | ✅ | Может авторизоваться |
| 👤 Старый | null | null | ❌ | Работает по телефону |

## Частые вопросы

### Q: Можно ли создать заказ для клиента без авторизации?
**A:** ✅ Да! Администратор может создавать заказы для любого клиента.

### Q: Что будет если клиент зарегистрируется с другим email?
**A:** ❌ Автосвязывание не сработает. Нужно будет связать вручную через `userId`.

### Q: Один пользователь может быть связан с несколькими клиентами?
**A:** ❌ Нет, связь 1:1. Один User = Один Client.

### Q: Можно ли удалить клиента со связью?
**A:** ✅ Да, но связь удалится (userId = null у User). Если есть заказы - нельзя удалить.

### Q: Как клиенту увидеть свои заказы после связывания?
**A:** Клиент авторизуется → система фильтрует Orders где client_id = его Client.id

## Что дальше?

### Уже готово:
- ✅ Схема базы данных
- ✅ API endpoints
- ✅ TypeScript типы

### Нужно создать UI:
1. Страница управления клиентами (admin)
   - Список клиентов
   - Создание/редактирование
   - Кнопка "Связать с пользователем"
   
2. Модальное окно связывания
   - Радио: Автоматически / Вручную
   - Селект пользователей (если вручную)
   
3. Страница "Мой аккаунт" (client)
   - Просмотр своих данных
   - История заказов
   - Создание нового заказа

## Документация

Подробная документация: **`CLIENT_USER_LINK.md`**

---

**Готово! Теперь можно создавать клиентов и связывать их с пользователями!** 🎉

