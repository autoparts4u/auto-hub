# Компонент OrderPaymentModal

## Описание

Отдельное модальное окно для управления оплатой заказа. Позволяет добавлять частичные платежи и отмечать заказ как полностью оплаченный.

## Расположение

```
components/orders/OrderPaymentModal.tsx
```

## Функционал

### Основные возможности:
- ✅ Просмотр деталей оплаты заказа
- ✅ Расчет итоговой суммы с учетом скидки
- ✅ Отображение оплаченной суммы и остатка
- ✅ Добавление частичных платежей
- ✅ Быстрая отметка заказа как полностью оплаченного
- ✅ Валидация введенной суммы
- ✅ Автоматическое обновление данных после операций

### Интерфейс

**Информация об оплате:**
- Сумма заказа
- Скидка (если есть)
- Итоговая сумма к оплате
- Оплаченная сумма
- Остаток к оплате
- Статус оплаты (полностью/частично)

**Действия:**
- "Добавить платеж" - добавляет частичный платеж
- "Оплачено полностью" - отмечает заказ как полностью оплаченный

## Props

```typescript
interface OrderPaymentModalProps {
  open: boolean;              // Открыто ли модальное окно
  onClose: () => void;        // Callback при закрытии
  orderId: string;            // ID заказа
  onPaymentUpdate: () => void; // Callback после успешной операции оплаты
}
```

## Использование

### В OrdersTable.tsx

Модальное окно открывается через кнопку с иконкой `DollarSign` в столбце "Действия":

```typescript
<Button
  variant="ghost"
  size="icon"
  onClick={() => handleOpenPayment(order)}
  title="Оплата"
>
  <DollarSign className="h-4 w-4" />
</Button>
```

### Подключение компонента

```typescript
import OrderPaymentModal from './OrderPaymentModal';

// В компоненте
const [isPaymentModalOpen, setIsPaymentModalOpen] = useState(false);
const [selectedOrder, setSelectedOrder] = useState<Order | null>(null);

const handleOpenPayment = (order: Order) => {
  setSelectedOrder(order);
  setIsPaymentModalOpen(true);
};

// В render
<OrderPaymentModal
  open={isPaymentModalOpen}
  onClose={() => {
    setIsPaymentModalOpen(false);
    setSelectedOrder(null);
  }}
  orderId={selectedOrder.id}
  onPaymentUpdate={fetchData}
/>
```

## Валидация

### Добавление платежа:
- ✅ Сумма должна быть больше 0
- ✅ Сумма не может превышать остаток к оплате
- ✅ Вводится в формате числа с плавающей точкой

### Отметка как оплаченного:
- ⚠️ Требуется подтверждение пользователя
- ✅ Автоматически рассчитывает и добавляет недостающую сумму

## Обработка ошибок

Все операции имеют обработку ошибок с выводом toast-уведомлений:
- ✅ Успешное добавление платежа
- ❌ Ошибка валидации суммы
- ❌ Ошибка загрузки данных заказа
- ❌ Ошибка добавления платежа
- ❌ Ошибка обновления статуса оплаты

## Зависимости

### Server Actions:
- `addPartialPayment` - добавление частичного платежа
- `markOrderAsFullyPaid` - отметка заказа как полностью оплаченного

### Утилиты:
- `calculateOrderTotal` - расчет итоговой суммы
- `calculateRemainingAmount` - расчет остатка к оплате
- `isOrderFullyPaid` - проверка полной оплаты

### UI компоненты:
- Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription
- Button, Badge, Label, Input, Separator
- Icons: DollarSign, CreditCard, CheckCircle2, AlertCircle, RefreshCw
- Toast уведомления (sonner)

## Дизайн

### Цветовое кодирование:
- 🟢 **Зеленый** - полностью оплачено
- 🟠 **Оранжевый** - частично оплачено
- 🔵 **Синий** - информация о клиенте

### Визуальные элементы:
- Иконка статуса оплаты (CheckCircle2 / AlertCircle)
- Бейдж со статусом
- Выделение ключевых сумм
- Separator для разделения секций

## Особенности реализации

1. **Автоматическое обновление:** После каждой операции оплаты данные заказа обновляются через API
2. **Callback onPaymentUpdate:** Позволяет родительскому компоненту обновить таблицу заказов
3. **Максимальная сумма:** Поле ввода ограничено остатком к оплате
4. **Автоочистка поля:** После успешного добавления платежа поле ввода очищается
5. **Disabled state:** Кнопки блокируются во время выполнения операций

## Пример workflow

1. Пользователь кликает на иконку 💵 в таблице заказов
2. Открывается модальное окно с деталями оплаты
3. Пользователь видит остаток к оплате
4. Вводит сумму частичного платежа (или нажимает "Оплачено полностью")
5. Система валидирует сумму
6. Платеж сохраняется в базе данных
7. Данные обновляются в модальном окне и в таблице
8. Показывается уведомление об успехе

## Преимущества отдельного компонента

✅ **Чистая архитектура** - разделение ответственности  
✅ **Переиспользуемость** - можно использовать в разных местах  
✅ **Читаемость** - меньше кода в одном файле  
✅ **Тестируемость** - легче тестировать изолированный компонент  
✅ **Maintainability** - проще поддерживать и обновлять  
