# Функционал системы заказов - Полная документация

## 🎉 Реализовано

Полный функционал для создания и контроля заказов со стороны администратора.

---

## 📁 Структура файлов

### API Эндпоинты

```
app/api/
├── orders/
│   ├── route.ts                    # GET (список), POST (создание)
│   ├── [id]/
│   │   ├── route.ts                # GET (детали), PUT (обновление), DELETE (удаление)
│   │   ├── status/
│   │   │   └── route.ts            # PUT (изменение статуса)
│   │   └── history/
│   │       └── route.ts            # GET (история статусов)
├── clients/
│   ├── route.ts                    # GET (список), POST (создание)
│   └── [id]/
│       └── route.ts                # GET, PUT, DELETE
├── order-statuses/
│   └── route.ts                    # GET (список), POST (создание)
└── delivery-methods/
    └── route.ts                    # GET (список), POST (создание)
```

### Компоненты UI

```
components/
├── orders/
│   ├── OrdersTable.tsx             # Таблица заказов с фильтрами
│   ├── OrderModal.tsx              # Создание нового заказа
│   └── OrderDetailsModal.tsx       # Детали заказа + изменение статуса
└── ui/
    ├── table.tsx                   # Компоненты таблицы (shadcn)
    └── textarea.tsx                # Компонент textarea (shadcn)
```

### Типы

```
app/types/
└── orders.ts                       # Все TypeScript типы для заказов
```

### Страницы

```
app/dashboard/
└── orders/
    └── page.tsx                    # Страница заказов
```

---

## 🚀 Основной функционал

### 1. Просмотр списка заказов (`OrdersTable`)

**Функции:**
- ✅ Отображение всех заказов в таблице
- ✅ Фильтрация по статусу
- ✅ Фильтрация по клиенту
- ✅ Поиск по номеру заказа, клиенту, трекинг-номеру
- ✅ Сортировка по дате создания (новые сверху)
- ✅ Отображение статуса с цветовой индикацией
- ✅ Отображение суммы заказа и скидки
- ✅ Отображение способа доставки
- ✅ Статистика: всего заказов, активных, завершенных, общая сумма

**Колонки таблицы:**
- № Заказа (короткий ID)
- Клиент (название + полное название)
- Статус (цветной badge)
- Позиций (количество)
- Сумма (с отображением скидки)
- Доставка (способ)
- Создан (дата и время)
- Действия (просмотр, удаление)

### 2. Создание заказа (`OrderModal`)

**Процесс создания:**

**Шаг 1:** Выбор основной информации
- Клиент * (обязательно)
- Статус * (по умолчанию "Новый")
- Способ доставки (из доступных для клиента)
- Скидка (в гривнах)
- Адрес доставки (автозаполнение из адреса клиента)
- Примечания

**Шаг 2:** Добавление позиций
- Поиск запчасти по артикулу
- Выбор склада
- Указание количества
- Указание цены
- Кнопка "Добавить позицию"

**Шаг 3:** Список позиций
- Отображение всех добавленных позиций
- Возможность удаления позиций
- Автоматический расчет:
  - Подытог
  - Скидка
  - **Итого**

**Бизнес-логика при создании:**
1. ✅ Проверка наличия товара на складе
2. ✅ Автоматическое списание товара со склада
3. ✅ Автоматический расчет `totalAmount`
4. ✅ Сохранение артикула и описания в `OrderItems` (защита данных!)
5. ✅ Создание первой записи в истории статусов
6. ✅ Привязка к текущему пользователю (`userId`)

### 3. Просмотр деталей заказа (`OrderDetailsModal`)

**Разделы:**

**A. Основная информация:**
- Клиент (название, полное название)
- Дата создания + менеджер
- Способ доставки + трекинг-номер
- Адрес доставки
- Текущий статус
- Сумма заказа (с скидкой)
- Дата выдачи (если выдан)
- Дата оплаты (если оплачен)
- Примечания

**B. Позиции заказа:**
- Артикул
- Описание
- Бренд
- Количество × Цена = Сумма
- Склад

**C. Изменение статуса:**
- Выбор нового статуса
- Поле для комментария
- Кнопка "Обновить статус"
- ⚠️ Недоступно для завершенных заказов (isLast: true)

**D. История изменений:**
- Временная линия (timeline)
- Все изменения статусов
- Дата и время каждого изменения
- Пользователь, выполнивший изменение
- Комментарии к каждому изменению

---

## 🔐 Безопасность и права доступа

### API уровень:

**Аутентификация:**
- ✅ Все эндпоинты требуют авторизацию (session)
- ✅ Проверка через `getServerSession(authOptions)`

**Авторизация:**
- ✅ `POST /api/orders` - только admin
- ✅ `PUT /api/orders/[id]` - только admin
- ✅ `DELETE /api/orders/[id]` - только admin
- ✅ `PUT /api/orders/[id]/status` - только admin
- ✅ `GET /api/orders` - любой авторизованный пользователь
- ✅ `GET /api/orders/[id]` - любой авторизованный пользователь

**Валидация:**
- ✅ Проверка обязательных полей
- ✅ Проверка наличия товара на складе перед созданием заказа
- ✅ Проверка существования связанных сущностей (клиент, статус, товары)
- ✅ Запрет удаления заказов в обработке
- ✅ Запрет изменения статуса завершенных заказов

---

## 📊 API Эндпоинты

### Заказы

#### `GET /api/orders`
Получить список заказов с фильтрацией

**Query параметры:**
- `status` - ID статуса
- `client` - ID клиента
- `dateFrom` - Дата от
- `dateTo` - Дата до
- `search` - Поиск по тексту

**Response:** `Order[]`

---

#### `POST /api/orders`
Создать новый заказ

**Request body:**
```typescript
{
  client_id: string;
  orderStatus_id: number;
  deliveryMethod_id?: number;
  notes?: string;
  deliveryAddress?: string;
  discount?: number;
  items: [
    {
      autopart_id: string;
      warehouse_id: number;
      quantity: number;
      item_final_price: number;
    }
  ];
}
```

**Response:** `Order` (201 Created)

**Транзакция выполняет:**
1. Создание заказа
2. Создание позиций с сохранением артикула и описания
3. Списание товара со склада
4. Создание записи в истории статусов

---

#### `GET /api/orders/[id]`
Получить детали заказа

**Response:** `Order` (с полными связями)

---

#### `PUT /api/orders/[id]`
Обновить заказ

**Request body:** `UpdateOrderDTO`

**Response:** `Order`

---

#### `DELETE /api/orders/[id]`
Удалить заказ

**Условия:**
- Только для статусов "Новый" или завершенных (isLast: true)
- Товары возвращаются на склад

**Response:** `{ message: string }`

---

#### `PUT /api/orders/[id]/status`
Изменить статус заказа

**Request body:**
```typescript
{
  orderStatus_id: number;
  comment?: string;
}
```

**Автоматические действия:**
- При статусе "Готов к выдаче" или "Выдан" → устанавливается `issuedAt`
- При статусе "Оплачен" → устанавливается `paidAt`

**Response:** `Order`

---

#### `GET /api/orders/[id]/history`
Получить историю статусов заказа

**Response:** `OrderStatusHistoryItem[]`

---

### Клиенты

#### `GET /api/clients`
Получить список клиентов

**Query параметры:**
- `search` - Поиск по названию

**Response:** `Client[]` (с количеством заказов)

---

#### `POST /api/clients`
Создать клиента

**Request body:**
```typescript
{
  name: string;
  fullName: string;
  address?: string;
}
```

**Response:** `Client` (201 Created)

---

#### `GET /api/clients/[id]`
Получить клиента с последними заказами

**Response:** `Client` (с последними 10 заказами)

---

#### `PUT /api/clients/[id]`
Обновить клиента

**Response:** `Client`

---

#### `DELETE /api/clients/[id]`
Удалить клиента

**Условие:** У клиента не должно быть заказов

**Response:** `{ message: string }`

---

### Статусы заказов

#### `GET /api/order-statuses`
Получить список статусов

**Response:** `OrderStatus[]`

---

#### `POST /api/order-statuses`
Создать новый статус (только admin)

**Request body:**
```typescript
{
  name: string;
  hexColor: string;
  isLast?: boolean;
}
```

**Response:** `OrderStatus` (201 Created)

---

### Способы доставки

#### `GET /api/delivery-methods`
Получить список способов доставки

**Query параметры:**
- `clientId` - Получить только методы для конкретного клиента

**Response:** `DeliveryMethod[]`

---

#### `POST /api/delivery-methods`
Создать способ доставки (только admin)

**Request body:**
```typescript
{
  name: string;
  hexColor?: string;
}
```

**Response:** `DeliveryMethod` (201 Created)

---

## 🎨 UI Компоненты

### OrdersTable

**Props:** Нет (все данные загружаются внутри)

**Состояния:**
- `orders` - массив заказов
- `statuses` - массив статусов
- `clients` - массив клиентов
- `loading` - индикатор загрузки
- Фильтры: `searchTerm`, `statusFilter`, `clientFilter`

**Функции:**
- `fetchData()` - загрузка данных
- `handleSearch()` - применение поиска
- `handleDelete(orderId)` - удаление заказа
- `handleViewDetails(order)` - открытие деталей

---

### OrderModal

**Props:**
```typescript
{
  open: boolean;
  onClose: () => void;
  clients: Client[];
  statuses: OrderStatus[];
}
```

**Основные состояния:**
- Информация о заказе
- Массив позиций `items`
- Списки: autoparts, warehouses, deliveryMethods

**Ключевые функции:**
- `handleAddItem()` - добавление позиции
- `handleRemoveItem(index)` - удаление позиции
- `calculateTotal()` - расчет итоговой суммы
- `handleSubmit()` - создание заказа

---

### OrderDetailsModal

**Props:**
```typescript
{
  open: boolean;
  onClose: () => void;
  orderId: string;
  statuses: OrderStatus[];
}
```

**Функции:**
- `fetchOrderDetails()` - загрузка деталей заказа
- `fetchHistory()` - загрузка истории
- `handleStatusUpdate()` - изменение статуса

**Особенности:**
- Timeline для истории статусов
- Блокировка изменения для завершенных заказов
- Автоматическое обновление после изменения статуса

---

## 💡 Примеры использования

### Создание заказа через API

```typescript
const createOrder = async () => {
  const orderData = {
    client_id: "client-uuid",
    orderStatus_id: 1, // "Новый"
    deliveryMethod_id: 2, // "Курьер"
    notes: "Срочная доставка",
    deliveryAddress: "г. Киев, ул. Тестовая, 1",
    discount: 100,
    items: [
      {
        autopart_id: "part-uuid-1",
        warehouse_id: 1,
        quantity: 2,
        item_final_price: 500
      },
      {
        autopart_id: "part-uuid-2",
        warehouse_id: 1,
        quantity: 1,
        item_final_price: 300
      }
    ]
  };

  const response = await fetch('/api/orders', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(orderData)
  });

  const order = await response.json();
  // totalAmount автоматически = (500*2 + 300*1) - 100 = 1200
};
```

---

### Изменение статуса заказа

```typescript
const updateOrderStatus = async (orderId: string) => {
  const statusData = {
    orderStatus_id: 2, // "В обработке"
    comment: "Заказ принят в обработку менеджером"
  };

  const response = await fetch(`/api/orders/${orderId}/status`, {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(statusData)
  });

  const updatedOrder = await response.json();
  // Автоматически создана запись в OrderStatusHistory
};
```

---

### Получение истории заказа

```typescript
const getOrderHistory = async (orderId: string) => {
  const response = await fetch(`/api/orders/${orderId}/history`);
  const history = await response.json();

  history.forEach(item => {
    console.log(`
      ${item.orderStatus.name}
      ${new Date(item.createdAt).toLocaleString()}
      ${item.user?.name}
      ${item.comment || ''}
    `);
  });
};
```

---

## 🔄 Жизненный цикл заказа в системе

```
1. [Создание]
   POST /api/orders
   ↓
   - Проверка наличия товара
   - Создание заказа (userId = текущий пользователь)
   - Создание позиций (с article и description)
   - Списание со склада
   - Создание записи истории
   ↓
   Статус: "Новый"

2. [Обработка]
   PUT /api/orders/{id}/status
   ↓
   Статус: "В обработке"
   ↓
   (Менеджер подбирает товары)

3. [Подготовка к отправке]
   PUT /api/orders/{id}/status
   ↓
   Статус: "Готов к выдаче"
   ↓
   - Автоматически устанавливается issuedAt
   - Если доставка: добавляется trackingNumber

4. [Выдача]
   PUT /api/orders/{id}/status
   ↓
   Статус: "Выдан"
   ↓
   (Клиент получил товар)

5. [Завершение]
   PUT /api/orders/{id}/status
   ↓
   Статус: "Оплачен" (isLast: true)
   ↓
   - Автоматически устанавливается paidAt
   ↓
   ЗАКАЗ ЗАВЕРШЕН
   (Дальнейшие изменения статуса запрещены)

   Альтернатива: "Отменен" (isLast: true)
   ↓
   ЗАКАЗ ОТМЕНЕН
```

---

## 🎯 Преимущества реализации

### 1. Защита данных
- ✅ OrderItems хранят article и description
- ✅ Даже если запчасть удалена из каталога, заказ сохраняет всю информацию
- ✅ Транзакции обеспечивают целостность данных

### 2. Полный аудит
- ✅ Каждое изменение статуса записывается
- ✅ Известно кто, когда и зачем изменил статус
- ✅ История доступна в реальном времени

### 3. Удобство для администратора
- ✅ Интуитивный интерфейс
- ✅ Все действия в несколько кликов
- ✅ Фильтрация и поиск по всем параметрам
- ✅ Статистика на главной странице

### 4. Безопасность склада
- ✅ Автоматическая проверка наличия
- ✅ Списание при создании заказа
- ✅ Возврат при удалении (если возможно)

### 5. Гибкость
- ✅ Настраиваемые статусы
- ✅ Различные способы доставки для разных клиентов
- ✅ Система скидок
- ✅ Возможность добавления любых примечаний

---

## 🚨 Важные замечания

### При удалении заказа:
- Можно удалить только заказы со статусом "Новый" или завершенные
- Товары автоматически возвращаются на склад

### При изменении статуса:
- Нельзя изменить статус завершенного заказа (isLast: true)
- Некоторые даты устанавливаются автоматически

### При удалении запчасти:
- Заказы сохраняют всю информацию
- autopart_id становится NULL, но article и description остаются

### При удалении клиента:
- Невозможно, если есть заказы

---

## 📝 Следующие возможные улучшения

1. **Экспорт данных:**
   - PDF-накладные
   - Excel-отчеты
   - Печать заказов

2. **Уведомления:**
   - Email при изменении статуса
   - SMS-уведомления клиентам
   - Push-уведомления менеджерам

3. **Аналитика:**
   - Графики продаж
   - Топ товаров
   - Анализ клиентов

4. **Расширенные фильтры:**
   - По датам выдачи/оплаты
   - По сумме заказа
   - По способу доставки

5. **Частичная выдача:**
   - Возможность выдавать заказ частями
   - Отслеживание невыданных позиций

---

## ✅ Checklist готовности

- [x] База данных обновлена
- [x] API эндпоинты созданы
- [x] Типы TypeScript определены
- [x] Компоненты UI реализованы
- [x] Страница заказов работает
- [x] Безопасность настроена
- [x] Валидация реализована
- [x] Транзакции используются
- [x] История статусов работает
- [x] Защита данных реализована
- [x] Документация создана

---

## 🎉 Система готова к использованию!

Все функционалы для создания и контроля заказов со стороны администратора полностью реализованы и готовы к работе.



