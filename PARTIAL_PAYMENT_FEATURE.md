# Функционал частичной оплаты заказов

## Обзор

Добавлена возможность отслеживания частичной оплаты заказов и быстрого просмотра неоплаченных выданных заказов.

## Изменения в базе данных

### Миграция: `20251030155256_add_paid_amount_to_orders`

Добавлено новое поле в таблицу `Orders`:
- `paidAmount` (Float, default: 0) - сумма, которая была оплачена клиентом

```sql
ALTER TABLE "Orders" ADD COLUMN "paidAmount" DOUBLE PRECISION NOT NULL DEFAULT 0;
```

### Применение миграции

Когда база данных будет доступна, выполните:
```bash
npx prisma migrate deploy
```

Или для dev окружения:
```bash
npx prisma migrate dev
```

## API изменения

### GET /api/orders

Добавлен новый query параметр:
- `unpaidIssued=true` - фильтр для показа только неоплаченных выданных заказов

Пример:
```
GET /api/orders?unpaidIssued=true
```

Фильтр работает следующим образом:
1. Выбираются заказы где `issuedAt IS NOT NULL` (выданные)
2. Где `paidAt IS NULL` (не помечены как полностью оплаченные)
3. И `paidAmount < (totalAmount - discount)` (частичная оплата или нет оплаты)

### PUT /api/orders/[id]

Добавлена поддержка обновления поля `paidAmount`:

```json
{
  "paidAmount": 150.50,
  "paidAt": "2025-10-30T12:00:00Z" // Опционально, устанавливается при полной оплате
}
```

## UI изменения

### 1. OrdersTable (Таблица заказов)

#### Новая кнопка фильтра
- **Кнопка "Неоплаченные"** с иконкой DollarSign
- При активации подсвечивается оранжевым цветом
- Показывает только выданные заказы с неполной оплатой

#### Отображение суммы оплаты
В колонке "Сумма" теперь показывается:
- Общая сумма заказа
- Скидка (если есть) - зеленым цветом
- **Оплачено: XX** - с цветовой индикацией:
  - 🟢 Зеленый - полная оплата
  - 🟠 Оранжевый - частичная оплата

### 2. OrderDetailsModal (Детали заказа)

#### Новая секция "Оплата заказа"

Отображает:
- **Подытог** - общая сумма заказа
- **Скидка** - сумма скидки (если есть)
- **К оплате** - итоговая сумма с учетом скидки
- **Оплачено** - уже оплаченная сумма (с цветовой индикацией)

#### Добавление частичной оплаты

Если заказ не полностью оплачен, показывается:
- Сумма, которую еще нужно оплатить
- Поле ввода для суммы оплаты
- Кнопка "Добавить оплату"

При добавлении частичной оплаты:
1. Новая сумма добавляется к `paidAmount`
2. Если `paidAmount` >= `(totalAmount - discount)`, автоматически устанавливается `paidAt`
3. Показывается уведомление об успехе
4. Данные обновляются автоматически

## Типы TypeScript

### Обновленные интерфейсы

```typescript
export interface Order {
  // ... существующие поля
  paidAmount: number;
}

export interface UpdateOrderDTO {
  // ... существующие поля
  paidAmount?: number;
}

export interface OrderFilters {
  // ... существующие поля
  unpaidIssued?: boolean; // Неоплаченные выданные заказы
}
```

## Примеры использования

### 1. Просмотр неоплаченных выданных заказов

1. Откройте страницу заказов `/dashboard/orders`
2. Нажмите кнопку **"Неоплаченные"** (она подсветится оранжевым)
3. В таблице отобразятся только выданные заказы с неполной оплатой

### 2. Добавление частичной оплаты

1. Откройте детали заказа (кнопка с иконкой глаза)
2. Найдите секцию **"Оплата заказа"**
3. Введите сумму в поле "Сумма оплаты"
4. Нажмите **"Добавить оплату"**
5. Оплата будет добавлена к текущей сумме

### 3. Программное обновление оплаты через API

```javascript
// Добавить 100 к оплате заказа
const response = await fetch(`/api/orders/${orderId}`, {
  method: 'PUT',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    paidAmount: currentPaidAmount + 100
  })
});
```

## Логика полной оплаты

Заказ считается полностью оплаченным, когда:
```
paidAmount >= (totalAmount - discount)
```

При достижении полной оплаты:
- Автоматически устанавливается `paidAt` (дата полной оплаты)
- Цвет индикатора меняется на зеленый
- Поле для добавления оплаты скрывается

## Визуальная индикация

### Цвета оплаты:
- 🟢 **Зеленый** - `paidAmount >= (totalAmount - discount)` - полная оплата
- 🟠 **Оранжевый** - `paidAmount < (totalAmount - discount)` - частичная оплата
- ⚪ **Серый (скрыто)** - `paidAmount === 0` - нет оплаты

### Фильтр "Неоплаченные":
- 🟠 **Оранжевый фон** - фильтр активен
- ⚪ **Прозрачный** - фильтр неактивен

## Важные замечания

1. **Миграция требует применения** - не забудьте выполнить миграцию при подключении к БД
2. **Автоматическая установка paidAt** - при полной оплате через UI автоматически устанавливается дата
3. **Валидация** - сумма оплаты не может превышать остаток к оплате
4. **Фильтр работает постфактум** - для производительности фильтрация частичной оплаты происходит после получения данных из БД

## Будущие улучшения (опционально)

- История платежей (отдельная таблица для отслеживания каждого платежа)
- Экспорт отчета по неоплаченным заказам
- Автоматические уведомления клиентам о задолженности
- Интеграция с платежными системами


